import { css } from '@emotion/react';

const mainBox = css({
  borderRadius: '10px',
  border: '2px solid black',
  maxWidth: '750px',
  margin: 'auto',
  padding: '50px',
  marginBottom: '40px',
});

const styles = { mainBox };

export default styles;
